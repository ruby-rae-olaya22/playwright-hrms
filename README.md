

playwright
dotenv
npx playwright init-agents --loop=opencode
npx playwright init-agents --loop=claude
npx playwright init-agents --loop=vscode
--? happy, negative, boundary, performance, security, accessibility, visual, integration, regression, smoke, sanity, edge cases
-- right now, we are focusing on happy path testing, which means we are testing the most common and expected scenarios where everything works as intended. This helps us ensure that the basic functionality of our application is solid before we start exploring more complex and less common scenarios.  
